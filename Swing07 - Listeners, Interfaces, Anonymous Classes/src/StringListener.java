// An interface
// The use of an interface means that anything that uses this 
// class/interface MUST implement the methods below
public interface StringListener {
	public void textEmitted(String text);
}
